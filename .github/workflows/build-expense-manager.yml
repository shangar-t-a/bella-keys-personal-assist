name: Build Expense Manager Service

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Image tag (leave empty to use VERSION file)"
        required: false
        type: string
  push:
    branches:
      - main
    paths:
      - "services/expense-manager-service/VERSION"

jobs:
  build:
    uses: ./.github/workflows/reusable-docker-build.yml
    permissions:
      contents: read
      packages: write
    with:
      service_path: ./services/expense-manager-service
      image_name: expense-manager-service
      tag: ${{ inputs.version }}
      push: ${{ github.event_name != 'pull_request' }}
      image_description: "Expense Manager Service - Backend API for expense tracking"
